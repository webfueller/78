[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rehearsal-domains"
version = "0.2.0"
description = "Two things built on rehearsal: a workbench for file edits, and a mail twin."
requires-python = ">=3.9"
license = { text = "MIT" }
dependencies = ["rehearsal"]

[project.scripts]
workbench = "workbench.cli:main"
preflight = "preflight.cli:main"

[tool.setuptools]
packages = ["workbench", "preflight"]

[tool.setuptools.package-data]
preflight = ["web/*"]
